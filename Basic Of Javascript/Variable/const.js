// it is a constat not an variable who can modify
const name = "aryan";
console.log(name);

// name = "alpha"; we cannot do this
// we cannot change it name but we can modify the value
console.log("alpha " + name);

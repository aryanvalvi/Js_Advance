//use let instead of var because black scope vs function Scope
// syntax
let name = "aryan";
console.log(name);
name = "Alpha";
console.log(name);

// we cannot do like var
// let name = "aryan" if it declare before we cannot declare with the same name again but we can assign a new value to it
